#!/bin/sh
# ssh.tcl \
exec tclsh "$0" ${1+"$@"}
namespace eval Ssh {
	
}
proc SshPs {file {cmd ps} } {
    if	{[catch {exec python.exe $file $cmd} msg]} {
	    return [list 0 $msg {}]
        } else {
		return [list 1 $msg {}]
        }
     
}